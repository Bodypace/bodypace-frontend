/** @type {import('next').NextConfig} */
const nextConfig = {
  // TODO: remove below experimental section after upgrading to Next.js 14
  experimental: {
    appDir: true,
  },
};

module.exports = nextConfig;
